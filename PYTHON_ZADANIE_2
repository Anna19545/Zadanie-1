# Pobranie wyboru kierunku konwersji
kierunek = input("Wybierz kierunek konwersji (C - Celsjusz na Fahrenheit, F - Fahrenheit na Celsjusz): ").upper()

if kierunek == "C":
    # Konwersja Celsjusz -> Fahrenheit
    celsius = float(input("Podaj temperaturę w stopniach Celsjusza: "))
    fahrenheit = celsius * 1.8 + 32
    print(f"{celsius}°C = {fahrenheit}°F")

elif kierunek == "F":
    # Konwersja Fahrenheit -> Celsjusz
    fahrenheit = float(input("Podaj temperaturę w stopniach Fahrenheita: "))
    celsius = (fahrenheit - 32) / 1.8
    print(f"{fahrenheit}°F = {celsius}°C")

else:
    print("Niepoprawny wybór kierunku konwersji. Wybierz 'C' lub 'F'.")
